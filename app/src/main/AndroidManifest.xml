<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kelly.chiarotti.mareu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_people"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_people_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.meeting_form.FormMeetingActivity"></activity>
        <activity android:name=".ui.meeting_list.ListMeetingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>